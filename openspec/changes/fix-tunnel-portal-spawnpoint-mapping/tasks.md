## 1. Fix click handler mapping

- [x] 1.1 In `src/Main.elm`, swap `EastStation` and `WestStation` in the `TunnelPortalId` and `WestTunnelPortalId` click handlers, and update comments
- [x] 1.2 Verify Elm compiles and run `npx elm-test`
- [x] 1.3 Run `npx playwright test` to confirm no regressions
